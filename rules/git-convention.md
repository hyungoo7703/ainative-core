# Git Convention

- Conventional Commits 형식을 따른다
  - `feat:` 새 기능
  - `fix:` 버그 수정
  - `refactor:` 리팩토링
  - `docs:` 문서
  - `chore:` 빌드, 설정 등 기타
  - `test:` 테스트
- 커밋 메시지는 소문자로 시작, 마침표 없음
- 한 커밋에 한 가지 변경만 담는다
- 브랜치명: `feat/xxx`, `fix/xxx`, `chore/xxx`
